---
- name: Run cfg commands on routers
  hosts: cisco_routers
  gather_facts: false 
  connection: local

  tasks:
    - name: Config password encryption 
      ios_config:
        lines:
          - service password-encryption

- name: exmaple with several commands
  hosts: cisco_routers
  gather_facts: false 
  connection: local

  tasks:
    - name: send config commands
      ios_config:
        lines:
          - service password-encryption
          - no ip http server
          - no ip http secure-server
          - no ip domain lookup 
          